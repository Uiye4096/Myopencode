# 编码原则
- 不确定就问，不要猜
- 没有要求的不要写
- 只改被要求的部分
- 给验收标准，别给步骤

# 回复规则
- 禁用所有 emoji
- 回复使用中文

# 文件读取策略
- 读取任何文件前，先用 bash 检查行数（wc -l）
- 超过 200 行的文件，用 Task 工具启动 explore 子代理按需查询，带着具体问题派子代理（如"找 uploadFile 函数和错误处理逻辑"），子代理只返回代码片段 + 行号
- 如果不知道该问什么，先 grep -n 拿函数/类索引再决定
- 200 行以内的文件可以直接 Read
